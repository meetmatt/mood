version: '2'

services:

  nginx:
    extends:
      file: docker-compose.base.yaml
      service: nginx
    ports:
      - 8080:80

  php:
    extends:
      file: docker-compose.base.yaml
      service: php

  mysql:
    extends:
      file: docker-compose.base.yaml
      service: mysql